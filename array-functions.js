const users = [
	{
		name: "Mehmet",
		age: 10,
	},
	{
		name: "Mehmet",
		age: 29,
	},
	{
		name: "Murat",
		age: 35,
	},
];

// users.push("Ayşe");
// users.push("Ayşe");
// console.log(users);

// users.map((item) => {
// 	console.log(item.name);
// });

// const result = users.find((item) => item.name == "Mehmet" && item.age >= 20);
// console.log(result);

// const filtered = users.filter(({name, age}) => name == "Mehmet");
// console.log(filtered);

// const some = users.some((item) => item.age == 10);
// console.log(some);

// const every = users.every((item) => item.age >= 40);
// console.log(every);

const meyveler = ["elma", "armut", "muz"];
const isIncluded = meyveler.includes("portakal");
console.log(isIncluded);
